# ローカル完結型 会議録音・文字起こしツール

**自分が参加した会議の議事録を、音声を一切外部に出さずに作るための Windows ツールです。**

会議の音声をクラウドの文字起こしサービスに送ると、そこには自分だけでなく
**相手の声も含まれます**。社外に出せない話をしている時に、それは選びにくい。
このツールは録音から文字起こしまでを手元の PC だけで完結させます。

- **完全ローカル**: 音声も文字起こし結果も外部に送信されません。ネットワークを使うのは
  **初回のモデル取得のみ**です（取得済みのモデルはローカルからのみ読むため、
  以降は通信が発生しません）。
- **文字起こし**: faster-whisper (CTranslate2) を CPU で実行します。GPU は不要です。
- **話者が正確**: 相手の声（PC の出力）と自分の声（マイク）を**物理的に別ファイルへ録る**ため、
  声質からの推測ではなく確定します。取り違えが起きません。

Windows のオーディオ機能（WASAPI ループバック）で PC の出力音声を取得する方式なので、
Zoom / Google Meet / Teams など**会議ツールの種類を問わず同じように動きます**。

## 録音するときのお願い

このツールは **PC で再生されている音声すべて**、つまり**相手の発言も**記録します。

- **録音することを参加者に伝えてください。** 多くの会議ツールには録画通知の仕組みが
  ありますが、このツールは OS 側で音を取るためそれが働きません。**黙って録れてしまう**
  ということは、伝える責任がこちら側にあるということです。
- **地域や職場によっては、同意のない録音に法的・規程上の制約があります。**
  ご自身の状況をご確認ください。
- 録音・文字起こしの結果には、**他人の発言が含まれます**。取り扱いにご注意ください。

---

## 導入手順

1. [最新版の ZIP](https://github.com/Yu-7777/local-meeting-transcriber/releases/latest)
   をダウンロードする
2. ZIP を右クリック →「**すべて展開**」
3. 中の **`setup.bat` を右クリック → プロパティ →「許可する」にチェック → OK**
4. **`setup.bat` をダブルクリック**して、`Setup complete` が出るまで待つ
   （初回のみ数分〜数十分）
5. Windows キーを押して「**会議**」と入力 → Enter

以上です。**Python などの事前準備は要りません。**

- 手順 3 を飛ばすと「安全でない可能性のあるファイルをブロックしました」と出ます
  → 手順 3 をやってからもう一度 `setup.bat` を実行してください
- 手順 4 の途中で `Put a shortcut on the desktop too? [Y/N]` と聞かれます
  → デスクトップにアイコンが欲しければ `Y`、不要なら `N`（放置でも先へ進みます）

初回に約 2GB を取得します（内訳と詳細は[セットアップの詳細](#セットアップの詳細)）。

---

## 使い方（GUI）

スタートメニューの `会議録音・文字起こし`、または `gui.bat` から起動します。

<!-- ショートカットの作り直し。フォルダを移動した時などに使う -->
<details>
<summary>ショートカットを作り直す / デスクトップにも置く</summary>

```
rem スタートメニューのみ
.venv\Scripts\python.exe shortcut.py

rem デスクトップにも置く
.venv\Scripts\python.exe shortcut.py --desktop
```

デスクトップのアイコンが不要になったら普通に削除するだけです
（ショートカットなので、消してもツール本体には影響しません）。
</details>

```
┌─ 録音 ────────────────────────┐
│  デバイス（相手 / 自分）        │
│  保存先 / 録音の名前            │
│  [録音開始]  経過時間  状態     │
│  レベルメーター（相手 / 自分）  │
├─ 文字起こし ──────────────────┤
│  対象の録音  [更新][改名][削除] │
│  音声ファイルを直接選ぶことも可 │
│  出力先/モデル/言語/話者分離    │
│  [実行]  [出力先を開く]         │
├─ ログ ────────────────────────┤
│  進捗と結果                     │
└────────────────────────────────┘
```

- 録音中は**両方のメーターが振れているか**を必ず確認してください（振れて
  いなければデバイス選択が誤っています）。
- 文字起こしは時間がかかりますが、その間もウィンドウは操作できます（進捗が下部に出ます）。
- 結果はファイル出力です。「出力先を開く」でフォルダが開きます。

**「録音を停止したら、そのまま文字起こし」** にチェックを入れておくと、停止した
時点で文字起こしまで自動で走ります。会議が終わったら放置しておけば、戻ってきた
頃には議事録ができています。設定は記憶されます。

**「言語」** は既定が日本語です。英語の会議を日本語のまま処理すると、
翻訳ではなく無関係な日本語が出力されるので、必ず合わせてください。

**「相手を話者ごとに分ける」** にチェックを入れると、相手側を声質で
`話者1` `話者2` に分けます。人数は自動で推定します（実測でも指定しないほうが
良い結果でした）。うまく分かれない時の調整はコマンドラインから行います
（「相手が複数人いる場合」を参照）。

**「削除」** は選択中の録音を**ごみ箱へ移します**（完全削除ではないので元に戻せます）。
サイズと、消える文字起こしファイルを確認してから実行します。録音中・文字起こし中は
実行できません。

> 録音は消したいが議事録は残したい、という場合は「出力先」を録音とは別の場所に
> 設定してください。そうすれば録音フォルダを削除しても議事録は残ります。

以下は同じことをコマンドラインで行う場合の手順です。

---

## 使い方（コマンドライン）

### 1. デバイスを確認する（初回・機器を変えた時）

```
.venv\Scripts\python.exe check_devices.py
```

「ループバックデバイス」と「入力デバイス（マイク）」の index を確認します。

### 2. 録音する

会議を始める前に実行します。

```
record.bat
```

または明示的にデバイスを指定する場合:

```
.venv\Scripts\python.exe record.py --mic 24
```

録音中はレベルメーターが表示されます。停止は **Enter キー**（Ctrl+C でも可）。
`recordings\日時\` に `system.wav`（相手）と `mic.wav`（自分）が保存されます。

主なオプション:

| オプション | 説明 |
|---|---|
| `--mic <index>` | マイクを指定（既定＝Windows の既定入力） |
| `--loopback <index>` | ループバック元を指定（既定＝Windows の既定出力） |
| `--name <名前>` | 録音に名前を付ける（フォルダ名が `日時_名前` になる） |
| `--seconds <N>` | N 秒で自動停止 |
| `--outdir <path>` | この回だけ保存先を変える |

### 録音の名前

録音フォルダは `2026_08_05_14_30` のように**日時**で作られます。GUI の
「名前」欄（または `--name`）に入れると `2026_08_05_14_30_定例MTG` になります。

日時を先頭に置いているのは、名前順に並べた時に時系列順になるためです。
後から GUI の「改名」ボタンで名前だけを付け替えられます（日時は変わりません）。
録ってから名前を決めても問題ありません。

- Windows で使えない文字（`\ / : * ? " < > |`）は `_` に置き換えます
- 同じ分に録り直した場合は `_2` `_3` と連番が付き、**前の録音を上書きしません**

**保存先の既定は `config.json` に記憶されます。** GUI の「保存先」欄で
「変更」を押すと、次回以降もそこに保存されます（1 時間の会議で約 1.3GB
使うので、容量に余裕のあるドライブを指定できます）。

**録音の保存先と、文字起こしの出力先は別々に指定できます。**
録音（GB 単位・使い終わったら消す）と議事録（KB 単位・残す）は
性質が違うため、たとえば「録音は外付けドライブ、議事録は同期フォルダ」
といった分け方ができます。詳細は次節。

### 3. 文字起こしする

録音とは別に、後から実行します（CPU 処理のため時間がかかります）。

```
.venv\Scripts\python.exe transcribe.py
```

引数なしで**最新の録音**を処理します。対象は自由に指定できます。

```
rem 特定の録音フォルダ
.venv\Scripts\python.exe transcribe.py recordings\2026_08_05_14_30_定例MTG

rem 手元の音声ファイルを直接（IC レコーダーの録音なども可）
.venv\Scripts\python.exe transcribe.py "D:\会議\打ち合わせ.mp3"

rem 出力先を分ける
.venv\Scripts\python.exe transcribe.py "D:\会議\打ち合わせ.mp3" --outdir "D:\議事録"
```

対応形式は wav / mp3 / m4a / flac / ogg / aac / wma と、mp4 / mkv / webm / mov
（動画から音声を取り出します）。

単体ファイルを指定した場合、結果は `<元のファイル名>_transcript.txt` として
出力されます。話者は「自分／相手」に分けられないため、`--diarize` を付けた時のみ
`話者1` `話者2` が付きます。

GUI では「音声ファイルを選ぶ...」ボタンから同じことができます。

主なオプション:

| オプション | 既定 | 説明 |
|---|---|---|
| `--model` | `large-v3-turbo` | `large-v3` にすると精度が上がるが数倍遅い（初回のみ 2.9GB の取得あり） |
| `--language` | `ja` | `auto` で自動判定、`en` で英語 |
| `--threads` | 自動 | CPU スレッド数（既定は[処理時間の目安](#処理時間の目安)を参照） |
| `--outdir` | 設定値 | 出力先。未設定なら入力と同じ場所 |
| `--offline` | off | 取得済みモデルのみ使用（ネットワークに一切触れない） |
| `--diarize` | off | 相手側を声質で話者ごとに分ける（相手が複数人の時） |
| `--speakers` | 自動 | 相手側の人数。**まず自動を試すこと**（下記の実測を参照） |

出力は `transcript.txt` の 1 ファイルだけです。

```
[00:00:03] 相手: 本日はお時間いただきありがとうございます。
[00:00:08] 自分: よろしくお願いします。
```

### 出力先を録音とは別の場所にする

既定では録音フォルダの中に出力しますが、GUI の「出力先」欄（または
`--outdir`）で別の場所を指定できます。指定は `config.json` に記憶され、
「既定」ボタンで元に戻せます。

**出力先を録音フォルダの外にすると、ファイル名に録音名が前置きされます。**

```
録音フォルダ内（既定） : transcript.txt
別の場所を指定した場合 : 2026_08_05_14_30_定例MTG_transcript.txt
```

すべての録音が `transcript.txt` という同じ名前で出ると、同じフォルダに
まとめた時に**先に作ったものが上書きされて消える**ためです。
単体ファイルを指定した場合も同様に `<元のファイル名>_transcript.txt` になります。

優先順位は **`--outdir` > `config.json` の設定 > 入力と同じ場所** です。

### 4. 相手が複数人いる場合（話者分離）

`自分` と `相手` はデバイスで物理的に分かれているため常に正確ですが、
相手側に複数人いると全員が `相手` にまとまります。
`--diarize` を付けると、相手チャンネルを声質で話者ごとに分けます。

```
.venv\Scripts\python.exe transcribe.py --diarize --speakers 3
```

```
[00:00:00] 相手(話者1): お疲れさまです。それでは定例を始めます。
[00:00:09] 相手(話者2): こちらは認証まわりの実装が終わりまして、今テスト環境で確認しています。
[00:00:18] 相手(話者1): ありがとうございます。何か懸念点はありますか。
[00:00:23] 自分: レート制限の件、こちらでも確認しておきます。
```

**まず自動のまま試してください。** `--speakers` での人数指定は必須ではなく、
実測では**指定しないほうが良い結果**でした（下記）。

| オプション | 既定 | 説明 |
|---|---|---|
| `--speakers N` | 自動 | N 人ちょうどに強制。自動が外れた時だけ使う |
| `--diar-threshold` | `0.9` | 大きいほど統合が進み**人数が減る**。小さいと増える |

**チューニングの実測データ**（YouTube の 3 人の対談・5 分で計測）

| threshold | 検出人数 | 内訳 |
|---|---|---|
| 0.5（旧既定） | **25 人** | 104s 54s 47s 35s 11s 6s … +1秒未満が8個 |
| 0.7 | 14 人 | 117s 107s 33s 15s … |
| **0.9（現在の既定）** | **3 人** | **125s / 118s / 48s** |
| `--speakers 3` 強制 | 3 人 | 167s / 126s / 3s ← 2 人分が融合して悪化 |

閾値 0.9 に加えて、**合計発話が 3 秒未満のクラスタは自動的に除外**します
（BGM・効果音・場面転換の音質差が「話者」として数えられるのを防ぐため）。

**精度について正直な注意**

- 会議音声はコーデック圧縮とノイズ抑制で劣化しているため、**取り違えは起こります**。
  特に発言の被り、相槌、ごく短い返事に弱いです。確定情報ではなく参考情報として扱ってください。
- 人数が実態と合わない時は `--diar-threshold` を 0.05 刻みで調整するのが有効です。
  それでも合わなければ `--speakers` で強制してください。
- 出せるのは `話者1` `話者2` という区別までで、**実名は出せません**。
- `自分` 側は分離不要なので処理しません。そのぶん一般的な話者分離より条件は良好です。

---

## セットアップの詳細

`setup.bat` が行うことは 4 つです。

1. Python を探す。無ければ公式インストーラを取得して導入（約25MB、管理者権限は不要）
2. `.venv` を作成
3. 依存パッケージを導入（約350MB）
4. 音声認識モデルを `models/` に取得（**約1.6GB**）

既に Python 3.10〜3.13 が入っていれば、それをそのまま使います（新たに入れません）。

**取得するのは既定のモデル（`large-v3-turbo`）だけです。** 精度優先の
`large-v3`（2.9GB）は、初めて選んだ時にその場でダウンロードします。GUI では
未取得のモデルを選ぶと注記が出て、実行前に確認を求めます。先に取っておきたい
場合は次を実行してください。

```
.venv\Scripts\python.exe download_models.py large-v3
```

### なぜ `setup.bat` だけ「許可する」が要るのか

インターネットから落としたファイルには「Web 由来」の印が付き、エクスプローラーで
展開すると**中の全ファイルに伝播**します。その状態の `.bat` は
スマート アプリ コントロールに**「Web からの危険なファイル拡張子」として確実に
ブロック**されます。

**手作業が要るのは `setup.bat` だけです。** 残りのファイルは `setup.bat` が
起動時に自分で解除します。`git clone` で取得した場合は、そもそも印が付かないため
この操作自体が不要です。

### なぜ Python の公式インストーラなのか

Python 本体には **PSF の署名**が付いており、スマート アプリ コントロールを
確実に通過します。uv などが配布するサードパーティ製の Python は署名がなく、
実測で `_ssl.pyd` がブロックされて動作しませんでした。

---

## 完全オフラインで運用する

**モデル取得後は何もしなくてもネットワークに触れません。** 取得済みのモデルは
必ずローカルだけから読むため、回線を遮断したままで動作します（Hub を到達不能に
して実測済み）。

`--offline` を付けると、未取得のモデルを指定した時に「黙って数 GB 落とし始める」
代わりにエラーで止まります。取得済みのモデルを使う限り、付けても付けなくても
挙動は同じです。

---

## 処理時間の目安

このツールはリアルタイム処理をしません。CPU（NVIDIA GPU 非搭載機）での目安:

| モデル | 60 分の会議の処理時間 | 用途 |
|---|---|---|
| `large-v3-turbo`（既定） | 概ね 30 分〜1 時間 | 通常はこちら |
| `large-v3` | 概ね 1.5〜3 時間 | 精度最優先。就寝中に回す想定 |

この PC での実測値（短いサンプル）は turbo が実時間の約 2.4 倍速、
large-v3 が約 1.1 倍速でした。2 本のファイルを順に処理するため、
上表はその合計です。

会話では各チャンネルの半分以上が無音になるため、VAD（無音除去）が効いて
実際にはこれより短く終わることが多いです。逆に発言が途切れなく詰まっている
録音では上表より長くかかります。

---

## パラメータの根拠（実測）

コード中の定数は、いずれもこの PC（i7-1260P / `large-v3-turbo`）で測った結果に
基づいています。環境が違えば結論も変わるので、変えたい時はここを測り直して
ください。使った録音は合計 4 時間ぶん（57 秒・22 分・76 分・2.4 時間）です。

### `threads = min(8, コア数)`

| スレッド | 60 秒の音声 |
|---|---|
| 4 | 37.0s |
| 8 | **24.8s** |
| 12 | 26.9s |

12 が遅いのは、P コアを使い切ったあと E コアを掴むためです。`config.json` の
`threads` で変更できます（速い CPU なら上げる価値があります）。

> **既に `config.json` がある場合**、そこに保存済みの `threads` が優先されます。
> 新しい既定を使いたい場合は、`config.json` の `threads` の行を消してください。

### `beam_size = 5`（ライブラリ既定のまま）

1 に下げると **1.26 倍速く**なりますが、60 秒の音声で誤りが増えました。
「面接の評価に**合わせて**」が「にあわって」、「リアルな面接に**する**前に」が
「に移する前に」になるなど、差が出た 5 行のうち 3 行で 5 のほうが正確でした。
会議は録り終えてから回すので、26% の時間より正確さを取っています。

なお 3 は速度・精度とも中途半端なうえ、18 秒ぶんの発言が 1 区間に融合しました。
区間が長いと話者ラベルの粒度が落ちるため、避けています。

### `compute_type = "int8"`

| | 認識時間 | 比 | 読み込み | 出力の一致率 |
|---|---|---|---|---|
| `int8` | 85.3s | 1.00x | 6.8s | — |
| `int8_float32` | 85.6s | 1.00x | 8.4s | 100.00% |
| `float32` | 188.9s | **0.45x** | 35.1s | 94.96% |

`float32` は **2.2 倍の時間**がかかって出力は 95% 同じです。`int8_float32` は
`int8` と完全に同一の出力で、速くもなりません。

### `min_silence_duration_ms = 500`（ライブラリ既定は 2000）

VAD が Whisper に渡す音声の量（＝処理量）を全録音で測りました。

| 録音 | 500ms | 2000ms |
|---|---|---|
| 57 秒 | 102.6s | 105.2s |
| 22 分 | 1,319.3s | 1,320.5s |
| 76 分 | 3,022.2s | 3,207.7s |
| 2.4 時間 | 7,243.7s | 7,679.6s |
| **合計** | **11,688s** | **12,313s** |

500ms のほうが **625 秒（5.3%）多く無音を捨てます**。差は無音の多い長い会議ほど
大きく、22 分の素材（発言が詰まった動画）ではほぼ差が出ませんでした。
文字起こし結果は 431 区間 / 8,853 文字 に対し 433 区間 / 8,861 文字 で、実質同じです。

### `MIN_SPEAKER_SEC = 3.0`

BGM や音質の変化が「別の話者」として検出されるのを防ぐ足切りです。実会議
11 分ぶんで測ったクラスタの発話長:

| クラスタ | 発話長 | 判定 |
|---|---|---|
| 1 番目 | 237.13s | 残る |
| 2 番目 | 70.66s | 残る |
| 3 番目 | 26.43s | 残る |
| 4 番目 | 9.30s | 残る |
| 5 番目 | 5.27s | 残る |
| 6〜11 番目 | 2.48s / 1.62s / 1.57s / 1.33s / 0.71s / 0.35s | 落ちる |

**2.48 秒と 5.27 秒の間に断絶があり、3.0 はその谷にあります。** 足切りが無いと、
この区間の話者は 5 人ではなく 11 人と報告されます。

### 話者の割り当てに重なりの下限は設けていない

同じ実会議で「採用した重なり ÷ 区間の長さ」を測ると、中央値 0.630、最小 0.040 で、
22% の区間が 0.30 を下回りました。ただし低いものはすべて長大な区間です
（65.9 秒の区間に「1回もこれでまとめますか」だけ、など）。

これは **Whisper が無音をまたいで区間の終端を伸ばすため**で、話者の判定が
怪しいわけではありません。割合で足切りすると、時刻が伸びただけの正しい区間から
ラベルを剥がすことになるため、下限は置いていません。

### `no_speech_prob` による除外は廃止しました

以前は `no_speech_prob > 0.8` の区間を捨てていましたが、**937 区間（22 分の動画 +
実会議 76 分）で 1 件も該当しませんでした**。実会議側は 504 区間すべてがちょうど
`0.0000` です。

`vad_filter=True` により Whisper は VAD が「発話」と判定した音声しか見ないため、
「無音への幻聴」は原理的に発生しません。重複行の除去（`text == prev_text`）は
実際に発火しているので残しています。

### `GAP_TOLERANCE = 0.15`

「録音の同期について」を参照してください。2 本の開始時刻の実測差
（0.26〜0.36 秒）を下回っている必要があります。

---

## 録音の同期について

2 本の WAV は別々のデバイスから録るため、開始時刻がそろいません。実測（4 時間
ぶん・8 ストリーム）では次のようになりました。

| | 先頭の起動遅れ | 途中の欠落補完 |
|---|---|---|
| 相手（ループバック） | 常に 0 秒 | 1.1 / 0 / **20.8** / 0.7 秒 |
| 自分（マイク） | 0.30 / 0.26 / 0 / 0.36 秒 | 常に 0 秒 |

- **補完が相手側にしか出ない**のは、WASAPI ループバックが「何も再生されていない
  間はデータを返さない」ためです。沈黙のぶんだけ穴が空くので、無音を書いて
  時間軸を保っています（76 分の録音で 20.8 秒ぶん）。
- **起動遅れが自分側にしか出ない**のは、マイクの初期化がループバックより
  0.3 秒ほど遅いためです。この差を先頭の無音で吸収しています。

録音長と実時間のズレは最大 0.59 秒（76 分中、0.013%）でした。

---

## 注意点・既知の制約

1. **録音中に音声出力デバイスを切り替えないでください。**
   Bluetooth ヘッドセットの接続・切断などを行うとループバックが途切れます。
   会議前にデバイスを固定してから録音を開始してください。

2. **ループバックは PC の音すべてを拾います。**
   通知音・BGM・他アプリの音も入ります。会議中は通知をオフにすることを推奨します。

3. **ヘッドフォン／イヤホンの使用を推奨します。**
   スピーカーで再生すると相手の声がマイクにも回り込み、両チャンネルに同じ発言が
   二重に出ます。ヘッドフォンなら分離がクリーンになります。

4. **Bluetooth ヘッドセットは音質が落ちる場合があります。**
   会議アプリがハンズフリー（HFP）モードに切り替えるとマイク音質が大きく低下し、
   文字起こし精度に直結します。可能なら有線ヘッドセットか PC 内蔵マイクを推奨します。

5. **初回はマイクへのアクセス許可を求められます。**
   Windows のダイアログで「許可」を押すまで、マイクは音を返しません。
   **押すまでの間の自分の声は録音されません。**
   一度許可すれば以降は出ないので、初回だけ会議開始前に短く録音して
   許可を済ませておくと確実です。

6. **デバイスの起動遅延は自動補正されます。**
   USB マイク等は録音開始から数秒遅れて音を返し始めることがあります。
   遅延分の無音を先頭に詰めて 2 本の WAV を同一時間軸に揃えるため、
   **トランスクリプト上の時刻はずれません**（上の 5 も同じ仕組みで吸収します）。

7. 録音終了時に「取りこぼしの可能性」と表示された場合は、途中で音が欠けた
   おそれがあります。他アプリの負荷を下げて再試行してください。

8. **「アプリケーション制御ポリシーによってこのファイルがブロックされました」と出た場合**
   （Windows セキュリティの通知も併せて飛んできます）

   **スマート アプリ コントロール (SAC)** によるブロックです。署名のない
   未知のコードを実行させない機能で、Windows 11 をクリーンインストールした
   環境でのみ有効になります。**個別に許可する仕組みはありません。**

   **最も多いのは `.bat` のブロックです。** ダウンロードした ZIP を展開すると
   「Web 由来」の印が全ファイルに伝播し、SAC が `setup.bat` を弾きます。
   これは[導入手順](#導入手順)の手順 3 で解決します。

   それでもブロックが出た場合は、まず `setup.bat` を再実行してください
   （評価の確認が間に合っていないだけのことがあります）。

   判定の仕組みと、依存パッケージをすべてバージョン固定している理由は
   [BUILD.md](BUILD.md) を参照してください。
   自分の PC で SAC が有効かどうかは、Windows セキュリティ →
   「アプリとブラウザーの制御」に「スマート アプリ コントロール」の項目が
   あるかどうかで分かります。項目が無ければ対象外です。

---

## 構成

```
LICENSE            MIT ライセンス
app.py             サブコマンドの振り分け（GUI もここから呼ばれる）
apppaths.py        実行形態（ソース / exe）によらないパス解決
config.py          設定の読み書き（config.json）
gui.py / gui.bat   簡易 GUI（録音と文字起こしの実行のみ。結果はファイル出力）
record.py          2 ストリーム同時録音 → system.wav / mic.wav / meta.json
transcribe.py      文字起こし → transcript.txt
diarization.py     相手チャンネルの話者分離 (sherpa-onnx / --diarize 時のみ)
check_devices.py   WASAPI デバイス一覧
download_models.py モデル事前取得
shortcut.py        スタートメニューへの登録（--desktop でデスクトップにも）
common.py          各所から使う小物（時刻整形・録音一覧・実行方法の案内）
setup.bat          初期セットアップ
record.bat         録音のショートカット
models/            Whisper モデル（初回取得後はオフラインで動作）
recordings/        録音データ
```


---

## ライセンス

MIT License（[LICENSE](LICENSE)）

自由に使用・改変・再配布できます。無保証です。
