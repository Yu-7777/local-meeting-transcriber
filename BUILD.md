# 開発と保守のための資料

利用者向けの説明は [README.md](README.md) にあります。ここには
**触る人だけが必要とすること**（テスト、値の根拠、配布の事情）を置きます。

**Windows 専用**です。WASAPI ループバックに依存しているため、
macOS / Linux では動きません（移植ではなく作り直しになります）。

## テスト

```
.venv\Scripts\python.exe -m unittest discover -s tests
```

数秒で終わります。**モデルも録音も要りません** — 必要な入力はテストが
その場で作って消します（`tests/helpers.py`）。利用者の録音に依存させると
その PC でしか動かないテストになるためです。

| ファイル | 対象 |
|---|---|
| `tests/test_units.py` | 名前の整形・設定の読み書き・レベル計算・話者ラベル・モデル判定 |
| `tests/test_transcribe.py` | 入出力の組み立て（どの WAV をどこにどの名前で出すか）と出力書式 |
| `tests/test_gui.py` | 画面の組み立て、未取得モデルの経路、子プロセス出力の振り分け |
| `tests/test_entrypoints.py` | サブコマンドの振り分け、ショートカット、録音の削除 |

過去に取りこぼした不具合はテストに落としてあります。

- 未取得のモデルを選ぶと `AttributeError`（`pythonw` では画面に何も出ない）
- Windows の `print()` が `\r\n` を出すため、ログが全部「進捗」に分類される
- 録音フォルダの外に出すと、全部 `transcript.txt` になって上書きし合う

### 何が守られていないか

**録音そのものと、モデルを使う推論はテストしていません。** 音声装置と数 GB の
モデル、そして数十分が必要で、自動テストに載せる性質のものではないためです。
具体的には `RecordingSession` の録音ループ、`transcribe_stream`、
`diarization.diarize`、モデルのダウンロードです。ここを触った時は実機で
確認してください。

GUI のダイアログを開く操作（保存先の選択、改名、削除の確認）も通していません。

**`move_to_trash` だけは実物で確かめています。** 取り違えると録音が復元不能に
なるため、ごみ箱に実際に入ったかを PowerShell 経由で確認しています。
そのぶん、テストを流すとごみ箱に一時フォルダが 1 つ残ります。

### テストに価値があることの確認

本体を 19 通りにわざと壊し、すべてでテストが落ちることを確認済みです
（`\r` の扱い、ファイル名の決定、ごみ箱行きのフラグなど）。この手順は
実行に数分かかるためリポジトリには入れていません。テストを大きく変えた時は、
同じことを手で確かめてください。

## パラメータの根拠（実測）

コード中の定数は、いずれもこの PC（i7-1260P / `large-v3-turbo`）で測った結果に
基づいています。変えたい時はここを測り直してください。
使った録音は合計 4 時間ぶん（57 秒・22 分・76 分・2.4 時間）です。

実測（短いサンプル）で `large-v3-turbo` は実時間の約 2.4 倍速、`large-v3` は
約 1.1 倍速でした。相手／自分の 2 本を順に処理するため、README の目安表は
その合計です。

### `threads = min(8, コア数)`

| スレッド | 60 秒の音声 |
|---|---|
| 4 | 37.0s |
| 8 | **24.8s** |
| 12 | 26.9s |

12 が遅いのは、P コアを使い切ったあと E コアを掴むためです。`config.json` の
`threads` で変更できます（速い CPU なら上げる価値があります）。

> **既に `config.json` がある場合**、そこに保存済みの `threads` が優先されます。
> 新しい既定を使いたい場合は、`config.json` の `threads` の行を消してください。

### `beam_size = 5`（ライブラリ既定のまま）

1 に下げると **1.26 倍速く**なりますが、60 秒の音声で誤りが増えました。
「面接の評価に**合わせて**」が「にあわって」、「リアルな面接に**する**前に」が
「に移する前に」になるなど、差が出た 5 行のうち 3 行で 5 のほうが正確でした。
会議は録り終えてから回すので、26% の時間より正確さを取っています。

なお 3 は速度・精度とも中途半端なうえ、18 秒ぶんの発言が 1 区間に融合しました。
区間が長いと話者ラベルの粒度が落ちるため、避けています。

### `compute_type = "int8"`

| | 認識時間 | 比 | 読み込み | 出力の一致率 |
|---|---|---|---|---|
| `int8` | 85.3s | 1.00x | 6.8s | — |
| `int8_float32` | 85.6s | 1.00x | 8.4s | 100.00% |
| `float32` | 188.9s | **0.45x** | 35.1s | 94.96% |

`float32` は **2.2 倍の時間**がかかって出力は 95% 同じです。`int8_float32` は
`int8` と完全に同一の出力で、速くもなりません。

### `min_silence_duration_ms = 500`（ライブラリ既定は 2000）

VAD が Whisper に渡す音声の量（＝処理量）を全録音で測りました。

| 録音 | 500ms | 2000ms |
|---|---|---|
| 57 秒 | 102.6s | 105.2s |
| 22 分 | 1,319.3s | 1,320.5s |
| 76 分 | 3,022.2s | 3,207.7s |
| 2.4 時間 | 7,243.7s | 7,679.6s |
| **合計** | **11,688s** | **12,313s** |

500ms のほうが **625 秒（5.3%）多く無音を捨てます**。差は無音の多い長い会議ほど
大きく、22 分の素材（発言が詰まった動画）ではほぼ差が出ませんでした。
文字起こし結果は 431 区間 / 8,853 文字 に対し 433 区間 / 8,861 文字 で、実質同じです。

### `MIN_SPEAKER_SEC = 3.0`

BGM や音質の変化が「別の話者」として検出されるのを防ぐ足切りです。実会議
11 分ぶんで測ったクラスタの発話長:

| クラスタ | 発話長 | 判定 |
|---|---|---|
| 1 番目 | 237.13s | 残る |
| 2 番目 | 70.66s | 残る |
| 3 番目 | 26.43s | 残る |
| 4 番目 | 9.30s | 残る |
| 5 番目 | 5.27s | 残る |
| 6〜11 番目 | 2.48s / 1.62s / 1.57s / 1.33s / 0.71s / 0.35s | 落ちる |

**2.48 秒と 5.27 秒の間に断絶があり、3.0 はその谷にあります。** 足切りが無いと、
この区間の話者は 5 人ではなく 11 人と報告されます。

### 話者の割り当てに重なりの下限は設けていない

同じ実会議で「採用した重なり ÷ 区間の長さ」を測ると、中央値 0.630、最小 0.040 で、
22% の区間が 0.30 を下回りました。ただし低いものはすべて長大な区間です
（65.9 秒の区間に「1回もこれでまとめますか」だけ、など）。

これは **Whisper が無音をまたいで区間の終端を伸ばすため**で、話者の判定が
怪しいわけではありません。割合で足切りすると、時刻が伸びただけの正しい区間から
ラベルを剥がすことになるため、下限は置いていません。

### `no_speech_prob` による除外は廃止しました

以前は `no_speech_prob > 0.8` の区間を捨てていましたが、**937 区間（22 分の動画 +
実会議 76 分）で 1 件も該当しませんでした**。実会議側は 504 区間すべてがちょうど
`0.0000` です。

`vad_filter=True` により Whisper は VAD が「発話」と判定した音声しか見ないため、
「無音への幻聴」は原理的に発生しません。重複行の除去（`text == prev_text`）は
実際に発火しているので残しています。

### `GAP_TOLERANCE = 0.15`

「録音の同期について」を参照してください。2 本の開始時刻の実測差
（0.26〜0.36 秒）を下回っている必要があります。

## 録音の同期について

2 本の WAV は別々のデバイスから録るため、開始時刻がそろいません。実測（4 時間
ぶん・8 ストリーム）では次のようになりました。

| | 先頭の起動遅れ | 途中の欠落補完 |
|---|---|---|
| 相手（ループバック） | 常に 0 秒 | 1.1 / 0 / **20.8** / 0.7 秒 |
| 自分（マイク） | 0.30 / 0.26 / 0 / 0.36 秒 | 常に 0 秒 |

- **補完が相手側にしか出ない**のは、WASAPI ループバックが「何も再生されていない
  間はデータを返さない」ためです。沈黙のぶんだけ穴が空くので、無音を書いて
  時間軸を保っています（76 分の録音で 20.8 秒ぶん）。
- **起動遅れが自分側にしか出ない**のは、マイクの初期化がループバックより
  0.3 秒ほど遅いためです。この差を先頭の無音で吸収しています。

録音長と実時間のズレは最大 0.59 秒（76 分中、0.013%）でした。

## サブコマンド

exe 単体で CLI としても動きます（GUI から文字起こしを呼ぶ際も同じ経路）。
引数なしで GUI、`MeetingTranscriber.exe devices / record / transcribe / download`
がそれぞれのサブコマンドです。オプションは [CLI.md](CLI.md) と同じです。

## セットアップの作りの理由

### なぜ `setup.bat` だけ「許可する」が要るのか

インターネットから落としたファイルには「Web 由来」の印が付き、エクスプローラーで
展開すると**中の全ファイルに伝播**します。その状態の `.bat` は
スマート アプリ コントロールに**「Web からの危険なファイル拡張子」として確実に
ブロック**されます。

**手作業が要るのは `setup.bat` だけです。** 次のどれでも確実に直せます
（すべて実測済み）。

| 方法 | 印 |
|---|---|
| **`setup.bat` だけを「許可する」** | それ以外は setup.bat が起動時に解除する (現在の手順) |
| ZIP を「許可する」してから展開 | 消える |
| `Expand-Archive` で展開する | そもそも伝播しない |
| `git clone` で取得する | そもそも付かない |

### なぜ Python の公式インストーラなのか

Python 本体には **PSF の署名**が付いており、スマート アプリ コントロールを
確実に通過します。uv などが配布するサードパーティ製の Python は署名がなく、
実測で `_ssl.pyd` がブロックされて動作しませんでした。

## 構成

```
LICENSE                            MIT ライセンス
app.py                             PyInstaller 用の薄いエントリスタブ
local_transcription/
    __init__.py
    app.py                         サブコマンドの振り分け（GUI もここから呼ばれる）
    apppaths.py                    実行形態（ソース / exe）によらないパス解決
    config.py                      設定の読み書き（config.json）
    gui.py                         簡易 GUI（録音と文字起こしの実行のみ。結果はファイル出力）
    record.py                      2 ストリーム同時録音 → system.wav / mic.wav / meta.json
    transcribe.py                  文字起こし → transcript.txt
    diarization.py                 相手チャンネルの話者分離 (sherpa-onnx / --diarize 時のみ)
    check_devices.py               WASAPI デバイス一覧
    download_models.py             モデル事前取得
    shortcut.py                    スタートメニューへの登録（--desktop でデスクトップにも）
    common.py                      各所から使う小物（時刻整形・録音一覧・実行方法の案内）
    device_watch.py                音声デバイスの着脱・既定変更をイベント駆動で監視 (GUI 用)
gui.bat                            GUI 起動
setup.bat                          初期セットアップ
record.bat                         録音のショートカット
build.spec                         PyInstaller のビルド定義
requirements.txt                   依存パッケージ（推移的な依存まですべてバージョン固定）
requirements-dev.txt               ビルド用（PyInstaller）
tests/                             テスト一式（本文「テスト」節を参照。モデルも録音も不要）
models/                            Whisper モデル（初回取得後はオフラインで動作）
recordings/                        録音データ
```

## ビルド

```
.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.venv\Scripts\python.exe -m PyInstaller build.spec --noconfirm
```

`dist\MeetingTranscriber\` にフォルダ一式ができます。配布先の PC に Python は
不要です。

## 配布物の構成

```
MeetingTranscriber\
├── MeetingTranscriber.exe      ← これを実行
├── _internal\                  ← Python 本体と DLL 群（触らない）
├── models\                     ← 別途コピーが必要（turbo だけなら約1.6GB）
└── recordings\                 ← 実行時に自動生成
```

**`models\` は exe に同梱していません。** turbo だけで 1.6GB、両方だと 4.5GB
あり、exe に含めるとビルドも起動も現実的でなくなるためです。配布時は次の
どちらかを選びます。

1. `models\` フォルダを一緒に配る（USB / 社内ファイルサーバ経由）
2. 配布先で初回に取得させる
   ```
   MeetingTranscriber.exe download --all
   ```

## onedir にした理由

`--onefile` にすると起動のたびに 300MB 超の DLL を temp へ展開するため、
起動が数十秒かかり、ネイティブ依存の多い構成では失敗も起きやすくなります。
市販ソフトと同じく「exe + DLL 群のフォルダ」形式にしています。

## 配布時の注意: コード署名

**このリポジトリは exe を配布していません。** ソース + `setup.bat` を配る形に
しています。理由は以下のとおりです。

### スマート アプリ コントロール (SAC) の判定ロジック

Microsoft の FAQ に明記されている順序:

1. **クラウドが「安全」と確信できる** -> 実行を許可
2. **クラウドが「悪意あり」と判断** -> ブロック
3. **クラウドが確信を持てない** -> 署名を見る
   - 有効な署名がある -> 許可
   - 無署名・署名が無効 -> **ブロック**

無署名バイナリは実行のたびに 3 番の抽選を受けます。署名の価値は「信用して
もらう」ことではなく、この非決定性を消すことにあります。

しかも無署名の評価は**ファイルのハッシュ**に紐づくため、リリースのたびに
ゼロに戻ります（Microsoft のドキュメントにも `new files that are unsigned have
no reputation by default` とあります）。署名があれば評価は**証明書**に貯まり、
リリースを重ねるほど信用が増します。

### 実測 (Windows 11 25H2 / SAC 有効)

ハッシュだけ変えた 30 個のコピーを用意し、うち 14 個を 1 回ずつ実行しました。

| 条件 | 試行 | 結果 |
|---|---|---|
| 初めて実行するファイル | 14 | **2 個がブロック (約 14%)** |
| 一度「安全」と判定されたファイル | 95 以上 | すべて成功 |
| 一度「ブロック」と判定されたファイル | 4 | **全部ブロック（覆らない）** |

**判定は一度確定するとキャッシュされ、覆りません。** 配布すると**受け取った人の
1 割前後が永久に起動できず**、SAC には個別許可の仕組みが一切ないため復旧手段も
ありません。これが exe を配布しない理由です。

### 署名しても解決しません

依存パッケージのネイティブバイナリ 117 個のうち、**署名があるのは 6 個だけ**です
（onnxruntime と NVIDIA 製の一部）。SAC は読み込むバイナリを個別に検査するため、
exe に署名しても、その中から無署名の `av` や `sherpa_onnx` を読んだ時点で
ブロックされます。実際に、PSF 署名済みの python.exe が無署名の `input.pyd` を
読んだところでブロックされました。

完全に解決するには 111 個すべてに署名し直す必要があり、現実的ではありません。

### 代わりにやっていること

`requirements.txt` で**推移的な依存まですべてバージョンを固定**し、SAC が
有効な Windows 11 で動作を確認済みの組み合わせだけを使います。ブロックされる
のは評価の付いていない新しいバージョンなので、これが最も効きます。

依存パッケージの無署名 DLL が通っているのは、PyPI 経由で世界中に配られていて
クラウドが判定を持っている (1 番で通る) ためです。自作の exe だけが判定を
持たず、3 番の抽選に晒されます。

なお **SAC が有効なのは Windows 11 をクリーンインストールした環境だけ**で、
開発者と判定されたユーザーでは自動的に無効になります。多数派は影響を受けません。

ソース配布側にも `.bat` が確定的にブロックされる問題がありますが、こちらは
確実に直せます（「なぜ `setup.bat` だけ「許可する」が要るのか」を参照）。

### 署名する場合

- 証明書は **RSA のみ**（ECC は非対応）。EV である必要はありません
- Trusted Signing (旧 Azure Code Signing) が Microsoft の推奨手段です
- **OSS なら SignPath Foundation が無料**です。個人へ証明書を出すのではなく
  「このバイナリはこの公開リポジトリからビルドされた」ことを保証する方式なので、
  身元確認が不要です。CI と連携させる必要があるため、配布規模が大きくなったら
  検討する価値があります
