# exe のビルドと配布

## ビルド

```
.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.venv\Scripts\python.exe -m PyInstaller build.spec --noconfirm
```

`dist\MeetingTranscriber\` にフォルダ一式ができます。

## 配布物の構成

```
MeetingTranscriber\
├── MeetingTranscriber.exe      ← これを実行
├── _internal\                  ← Python 本体と DLL 群（触らない）
├── models\                     ← 別途コピーが必要（turbo だけなら約1.6GB）
└── recordings\                 ← 実行時に自動生成
```

**`models\` は exe に同梱していません。** turbo だけで 1.6GB、両方だと 4.5GB
あり、exe に含めるとビルドも起動も現実的でなくなるためです。配布時は次の
どちらかを選びます。

1. `models\` フォルダを一緒に配る（USB / 社内ファイルサーバ経由）
2. 配布先で初回に取得させる
   ```
   MeetingTranscriber.exe download --all
   ```

## onedir にした理由

`--onefile` にすると起動のたびに 300MB 超の DLL を temp へ展開するため、
起動が数十秒かかり、ネイティブ依存の多い構成では失敗も起きやすくなります。
市販ソフトと同じく「exe + DLL 群のフォルダ」形式にしています。

## サブコマンド

exe 単体で CLI としても動きます（GUI から文字起こしを呼ぶ際も同じ経路）。

```
MeetingTranscriber.exe                      GUI
MeetingTranscriber.exe devices              デバイス一覧
MeetingTranscriber.exe record --seconds 60  録音
MeetingTranscriber.exe transcribe <dir> --diarize
MeetingTranscriber.exe download --all       モデル取得
```

## 配布時の注意: コード署名

**このリポジトリは exe を配布していません。** ソース + `setup.bat` を配る形に
しています。理由は以下のとおりです。

### スマート アプリ コントロール (SAC) の判定ロジック

Microsoft の FAQ に明記されている順序:

1. **クラウドが「安全」と確信できる** -> 実行を許可
2. **クラウドが「悪意あり」と判断** -> ブロック
3. **クラウドが確信を持てない** -> 署名を見る
   - 有効な署名がある -> 許可
   - 無署名・署名が無効 -> **ブロック**

無署名バイナリの運命は 3 番の分岐で決まり、そこに至るかどうかは
**実行のたびに行われるクラウド照会の結果**に依存する。ネットワークや時刻の
問題で照会が成立しないと 3 番に落ちる。つまり同じファイルでも結果が変わりうる。

**署名があると 3 番が「許可」に変わるため、クラウドへの依存が外れて確定的になる。**
署名の価値は「信用してもらう」ことではなく、この非決定性を消すことにある。

### 実測 (Windows 11 25H2 / SAC 有効)

本プロジェクトの exe を、ハッシュだけ変えた 30 個のコピーで試した結果:

| 条件 | 結果 |
|---|---|
| 初めて実行するファイル | **14 個中 2 個がブロック (約 14%)** |
| 一度「安全」と判定されたファイル | 95 回以上すべて成功 |
| 一度「ブロック」と判定されたファイル | **4 回再試行しても全部ブロック** |

**判定は一度確定するとキャッシュされ、覆らない。** ブロック側に転んだファイルは
そのマシンで二度と起動できず、利用者側に復旧手段がない (SAC には個別許可の
仕組みが一切ない)。

つまり配布すると、**受け取った人の 1 割前後が永久に起動できない**ことになる。
これが exe を配布しない理由。

### 「利用者が増えれば評価が付く」は成り立たない

SmartScreen / SAC の評価は次のように紐づく。

- **署名あり**: 評価は**証明書**に貯まる。リリースを重ねるほど信用が増す
- **署名なし**: 評価は**ファイルのハッシュ**に紐づく。リリースのたびにハッシュが
  変わるので**毎回ゼロから**。永遠に「未知のファイル」のまま

Microsoft のドキュメントにも `new files that are unsigned have no reputation by
default` とある。無署名で配り続けても状況は改善しない。

### 署名しても解決しません

依存パッケージのネイティブバイナリ 117 個のうち、**署名があるのは 6 個だけ**です
（onnxruntime と NVIDIA 製の一部）。SAC は読み込むバイナリを個別に検査するため、
exe に署名しても、その中から無署名の `av` や `sherpa_onnx` を読んだ時点で
ブロックされます。実際に、PSF 署名済みの python.exe が無署名の `input.pyd` を
読んだところでブロックされました。

完全に解決するには 111 個すべてに署名し直す必要があり、現実的ではありません。

### 代わりにやっていること

`requirements.txt` で**推移的な依存まですべてバージョンを固定**し、SAC が
有効な Windows 11 で動作を確認済みの組み合わせだけを使います。ブロックされる
のは評価の付いていない新しいバージョンなので、これが最も効きます。

依存パッケージの無署名 DLL が通っているのは、PyPI 経由で世界中に配られていて
クラウドが判定を持っている (1 番で通る) ためです。自作の exe だけが判定を
持たず、3 番の抽選に晒されます。

なお **SAC が有効なのは Windows 11 をクリーンインストールした環境だけ**で、
開発者と判定されたユーザーでは自動的に無効になります。多数派は影響を受けません。

### ソース配布側にも一点だけ問題があります

ダウンロードした ZIP をエクスプローラーで展開すると「Web 由来」の印が中身へ
伝播し、`.bat` は **「Web からの危険なファイル拡張子」として確定的にブロック**
されます。exe と違い、こちらは次のどれでも確実に直せます (すべて実測済み)。

| 方法 | 印 |
|---|---|
| **`setup.bat` だけを「許可する」** | それ以外は setup.bat が起動時に解除する (現在の手順) |
| ZIP を「許可する」してから展開 | 消える |
| `Expand-Archive` で展開する | そもそも伝播しない |
| `git clone` で取得する | そもそも付かない |

**「必ず起きるが必ず直せる」ソース配布のほうが、「たまに起きて絶対直せない」
exe より配布物として優れている**、というのが結論です。

### 署名する場合

- 証明書は **RSA のみ**（ECC は非対応）。EV である必要はありません
- Trusted Signing (旧 Azure Code Signing) が Microsoft の推奨手段です
- **OSS なら SignPath Foundation が無料**です。個人へ証明書を出すのではなく
  「このバイナリはこの公開リポジトリからビルドされた」ことを保証する方式なので、
  身元確認が不要です。CI と連携させる必要があるため、配布規模が大きくなったら
  検討する価値があります

## 前提

- **Windows 専用**です。WASAPI ループバックに依存しているため、
  macOS / Linux では動きません（移植ではなく作り直しになります）。
- 配布先の PC に Python は不要です。
